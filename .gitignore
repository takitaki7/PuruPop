node_modules/
.DS_Store

# Build output: regenerated by `npm run build` from the files in the repo
# root, so it is never the source of truth.
www/

# The generated ios/ project IS committed, so Xcode settings (signing,
# capabilities, orientation) survive. Capacitor writes its own
# ios/.gitignore covering the build artifacts inside it.
